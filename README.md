# Masoud Vahedi — Product Manager Portfolio

Open `index.html` in a browser. The site is responsive and uses plain HTML, CSS, and JavaScript.

## Before publishing

1. Replace the two placeholder `href="#"` values in the contact section with Masoud's email and LinkedIn URLs.

The supplied portrait and three personal photographs are already included in `assets/` and used throughout the page.

## Files

- `index.html` — content and page structure
- `styles.css` — visual system and responsive layout
- `script.js` — navigation, reveals, counters, career expansion, and card interaction
